@model ToonSaloon.Models.CartoonOfTheDay
@{
    ViewBag.Title = "AdminDeleteToon";
}

@section styles{
    <link rel='stylesheet' href='../../Content/app/HomePageStyles.css' type='text/css' />
}
<br />

<div class="col-xs-4 bpost">
    <h2 class="text-center">Edit Toon</h2>
    @using (Html.BeginForm("AdminDeleteToon", "Admin", FormMethod.Post, new { enctype = "multipart/from-data" }))
    {
        @Html.HiddenFor(t => t.Id)
        @Html.HiddenFor(t=>t.DateCreated)
        <div class="form-group">
            <label>Show Name</label>
            @Html.DisplayFor(t => t.ShowName, new { @class = "form-control" })
        </div>

        <div class="form-group">
            <label>Season</label>
            @Html.DisplayFor(t => t.Season, new { @class = "form-control" })
        </div>

        <div class="form-group">
            <label>Episode</label>
            @Html.DisplayFor(t => t.Episode, new { @class = "form-control" })
        </div>

        <div class="form-group">
            <label>Author</label>
            @Html.DisplayFor(t => t.Author, new { @class = "form-control" })
        </div>

        <button type="submit" class="col-sm-offset-2 btn-lg btn-success">Delete</button>
        @Html.ActionLink("Cancel", "ManageToonOfTheDay", "Admin", null, new { @class = "btn btn-sm btn-info" })

    }
</div>
